services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: bestcrm
      POSTGRES_USER: bestcrm
      POSTGRES_PASSWORD: bestcrm
    ports:
      - "5432:5432"
    volumes:
      - bestcrm_pgdata:/var/lib/postgresql/data

volumes:
  bestcrm_pgdata:
