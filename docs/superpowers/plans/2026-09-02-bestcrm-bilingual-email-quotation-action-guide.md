# BESTCRM 双语、询价、协作、报价及邮件中心分阶段行动指南

- 日期：2026-09-02
- 状态：第 5 步本地实施完成，等待用户确认
- 本地代码：`C:\Users\Mark\Documents\BESTCRM`
- 目标环境：腾讯云新加坡，`https://crm.sunkaier.com`
- 回退环境：南京 CRM，仅保留回退用途，本计划不得修改南京数据或服务

## 1. 实施目标

在不影响南京 CRM、不中断新加坡现有 CRM 的前提下，分阶段实现：

1. 登录后中英文切换和全部新增页面双语化。
2. 所有新商机先进入询价，由销售经理审核、查重、分配并转为商机。
3. 销售自行获得的线索可以快速提交，但不能绕过询价直接新建商机。
4. 每个商机设置一名项目主任工程师和多名协助工程师。
5. 建立受控的产品技术协议模板库，快速生成项目技术方案。
6. 技术方案、商务报价和完整报价包分别进行可追溯的版本管理。
7. 使用 `sales@sunkaier.com` 在 CRM 内收发业务邮件并归档。
8. 所有正式发送版本、邮件、附件、审批及操作记录可长期恢复。

详细业务规则以
`docs/superpowers/specs/2026-09-02-bestcrm-bilingual-email-center-design.md`
为准。本指南只规定实施顺序、验收标准和确认门。

## 2. 强制执行规则

### 2.1 一次只做一步

- 每次只实施下列一个步骤，不同时跨两个步骤修改。
- 当前步骤完成后，停止继续开发并提交验收报告。
- 只有用户明确回复“确认第 N 步”后，才能进入第 N+1 步。
- 用户提出调整时，只修正当前步骤并重新验收，不提前做后续功能。

### 2.2 本地、部署和启用是三个独立确认

- 第 1 至第 9 步默认只修改本地代码、数据库测试结构和本地预览。
- 本地步骤通过不代表允许部署到新加坡。
- 部署到新加坡不代表允许启用真实邮件收发。
- 南京服务器 `175.27.225.156` 始终为禁止写入目标，除非用户以后另行明确授权。
- 对新加坡进行任何远程写操作前，必须再次核对公网 IP、SSH 用户、应用目录、
  systemd 服务、数据库、附件目录、备份目录和当前版本。

### 2.3 每一步的完成标准

每一步必须同时具备：

1. 只包含本步骤范围的代码或文档变更。
2. 数据库迁移可重复执行，并保留旧数据兼容性。
3. 服务层、页面、GET/POST/API 和附件下载使用同一权限规则。
4. 针对性测试通过。
5. 完整 `npm test` 通过；如有既存失败，必须证明与本步骤无关并取得确认。
6. `git diff --check` 通过，`git status --short` 已复核。
7. 可见页面完成中英文浏览器验收。
8. 形成范围明确的 Git 检查点，不夹带无关用户文件。
9. 报告修改内容、测试结果、残余风险、回退方法和生产影响。

### 2.4 密码与密钥

- 不读取、不显示、不猜测 NetEase 邮箱授权密码、用户密码或 `.pem` 私钥。
- 密码由用户在私密终端或供应商界面输入。
- 凭据不进入 Git、数据库、日志、截图或聊天记录。

## 3. 当前基线

### 3.1 已核对事实

- 第 1 步本地 Git 检查点：`5248928`（基于生产基线标签 `v2026.09.02-01-4dd0d54`）。
- 第 2 步本地 Git 检查点：`23c0aeb`。
- 第 3 步本地 Git 检查点：`44f415c`。
- 第 4 步本地 Git 检查点：`6baac1a`。
- 询价目录已经限制为管理员和销售经理，当前测试覆盖导航及直接访问。
- 当前本地完整测试：`518` 通过，`0` 失败。
- 登录后语言切换已经作为第 1 步本地检查点交付。
- 本行动指南和综合设计文件已经纳入 Git，并随实施进度更新。
- 工作区存在其他历史或用户变更；后续每一步必须使用精确文件清单提交。
- 现有 `docs/operation-runbook.md` 仍记录南京服务器，不能直接用作新加坡部署指令。

### 3.2 第一次远程部署前必须补齐的环境清单

| 项目 | 南京回退环境 | 新加坡目标环境 |
| --- | --- | --- |
| 公网地址 | `175.27.225.156` | 当前记录为 `43.134.52.251`，部署前重新查询 |
| 域名 | 不再作为主入口 | `crm.sunkaier.com`，部署前重新解析确认 |
| SSH 用户 | 只读核对时另行授权 | 部署前核对 |
| 应用目录 | 禁止写入 | 部署前核对 |
| 服务名 | 禁止重启 | 部署前核对 |
| 数据库 | 禁止修改 | 部署前核对名称、用户及连接方式 |
| 附件目录 | 禁止修改 | 部署前核对路径和属主 |
| 备份目录 | 保留 | 部署前建立并验证 |

在这些值完成实时核对前，不得复制南京运维命令到新加坡执行。

## 4. 分阶段实施步骤

## 第 1 步：固化本地基线与双语基础

### 修改范围

- 复核并完成登录后侧栏 `English / 中文` 切换。
- 保证切换语言后返回当前页面，而不是丢失正在查看的商机。
- 给后续新模块建立统一翻译键命名规则和缺失翻译测试。
- 将本综合设计和本行动指南作为独立文档纳入范围。
- 不修改询价、商机、报价和邮件业务逻辑。

### 验收

- 登录页、工作台、客户、联系人、询价、商机、系统管理均可切换语言。
- 当前 URL 和筛选条件在切换后保留。
- 未登录用户不能通过语言路由获得受保护页面。
- 针对性路由测试和完整测试通过。
- 浏览器展示中英文侧栏和至少一个业务页面。

### 确认门

交付后停止，等待：`确认第1步，开始第2步`。

## 第 2 步：所有新商机统一经过询价

### 修改范围

- 增加销售人员的“提交新线索”简表，不授予询价目录权限。
- 记录来源渠道、客户/联系人、需求、产品、附件和提交人。
- 销售只能查看自己提交后的回执状态，不能浏览询价详情目录。
- 销售经理完成查重、客户确认、销售负责人分配和询价转商机。
- 增加销售经理“快速处理”，但仍在同一事务中建立询价和转换审计。
- 阻断销售人员直接通过页面或 JSON API 创建真实商机。
- 现有商机往来和补充需求不重新生成询价。

### 权限矩阵

| 操作 | 销售 | 销售经理 | 管理员 |
| --- | --- | --- | --- |
| 提交自己的新线索 | 允许 | 允许 | 允许 |
| 查看询价目录/详情 | 禁止 | 允许 | 允许 |
| 查重、分配、转商机 | 禁止 | 允许 | 允许 |
| 绕过询价新建真实商机 | 禁止 | 禁止 | 仅审计修复工具，不提供普通 UI |

### 验收

- 菜单、服务、GET、POST、JSON API、附件访问都执行相同权限。
- 销售直接访问询价 URL 返回拒绝，不能通过构造 POST 绕过。
- 一个线索只能转换一次，并保留来源和经理决定。
- 重复客户、重复邮件和重复平台编号不会重复创建商机。
- 全角色浏览器流程及完整测试通过。

### 确认门

交付后停止，等待：`确认第2步，开始第3步`。

## 第 3 步：商机工程协作角色

### 修改范围

- 每个商机设置且只能设置一名项目主任工程师。
- 一个商机可增加多名协助工程师。
- 支持按商机分配协助工程师的章节、任务和到期日。
- 记录新增、移除、角色变更和实际贡献人。
- 主任工程师整合并提交技术方案；协助工程师默认不能最终提交。
- 协助工程师默认只能起草对外邮件，单独授权后才可直接发送。

### 验收

- 同一人员可在不同商机承担不同工程角色。
- 移除工程成员后立即失去商机、文件和邮件访问权，但历史署名保留。
- 只有主任工程师可以提交完整技术方案。
- 直接 URL、附件和发送接口不能绕过权限。
- 角色矩阵、审计历史和完整测试通过。

### 确认门

交付后停止，等待：`确认第3步，开始第4步`。

## 第 4 步：产品技术协议模板库的数据与权限基础

### 修改范围

- 新增产品技术协议模板、模板修订、标准条款和变量定义表。
- 模板按产品族、型号、应用和语言分类。
- 模板状态为草稿、待审核、已发布、已停用。
- 使用 `TPL-R1`、`TPL-R2` 独立编号。
- 已发布模板不可覆盖；修改必须创建新修订。
- 管理员维护账号和权限，技术经理审核发布技术内容。

### 验收

- 只有已发布模板可以用于新技术方案。
- 修改模板不会改变已有项目快照。
- 停用模板不能再新建，但历史方案仍可查看。
- 变量白名单不允许脚本、宏或任意代码执行。
- 数据库约束、权限路由和完整测试通过。

### 确认门

交付后停止，等待：`确认第4步，开始第5步`。

## 第 5 步：模板编辑器与快速生成技术方案

### 修改范围

- 建立中英文模板编辑界面、章节排序、标准条款和条件章节。
- 支持参数表、设备表、材料、仪控、电气、公用工程和供货边界。
- 从客户、询价、商机和产品数据自动预填变量。
- 缺失必填参数、超出工程范围和修改标准条款时给出清晰提示。
- 主任工程师生成独立项目草稿；协助工程师编辑被分配章节。
- 保存模板修订、变量值、选中条款和渲染内容快照。

### 验收

- 从一个测试产品模板生成完整的英文、中文和双语方案。
- 项目内修改不会反向修改标准模板。
- 必填字段未完成时不能提交审批。
- 标准条款改动有差异提示和贡献人记录。
- 浏览器生成流程及完整测试通过。

### 确认门

交付后停止，等待：`确认第5步，开始第6步`。

## 第 6 步：技术方案版本、审批和文档输出

### 修改范围

- 工作草稿编号为 `TS-D1`、`TS-D2`。
- 提交并批准的正式技术方案编号为 `TS-V1`、`TS-V2`。
- 接入现有主任工程师提交、技术经理批准/退回流程。
- 退回后从原版本复制新草稿，不覆盖被退回或已批准版本。
- 输出带 SUNKAIER 品牌、文档编号、页码、版本和审批信息的 DOCX/PDF。
- 对输出文件计算校验和并绑定到技术方案版本。

### 验收

- 已批准版本内容和文件不可编辑或删除。
- DOCX/PDF 的关键字段、版本、表格和分页通过视觉检查。
- 中英文输出无缺字、乱码、越界或错误字体替换。
- 退回、修订、重提和历史查看完整可追溯。
- 文档生成测试、权限测试和完整测试通过。

### 确认门

交付后停止，等待：`确认第6步，开始第7步`。

## 第 7 步：完整报价包 V1、V2 版本管理

### 修改范围

- 一个报价包绑定准确的技术方案版本、商务报价、条款和附件。
- 内部草稿使用 `QP-D1`、`QP-D2`；正式对客版本使用 `QP-V1`、`QP-V2`。
- V2 从 V1 克隆，必须填写修改原因和客户可读的变更摘要。
- 增加价格、交期、付款、有效期、条款和附件的版本差异比较。
- 已发送、已替代和已接受的报价包不可覆盖或删除。
- 成单时必须明确记录客户接受的报价包版本。

### 验收

- 技术版本未批准时，报价包不能提交或发送。
- V2 发出后 V1 显示“已替代”，但全部内容、文件和历史仍存在。
- 客户接受版本与合同/订单明确关联。
- 版本并发、重复编号、直接修改和删除均被数据库及服务层阻断。
- 完整版本流程及完整测试通过。

### 确认门

交付后停止，等待：`确认第7步，开始第8步`。

## 第 8 步：邮件接收、会话和永久归档基础

### 修改范围

- 建立邮件会话、邮件、附件和投递尝试数据结构。
- 通过 NetEase IMAP/TLS 接收邮件，不删除邮箱服务器邮件。
- 使用 Message-ID、In-Reply-To、References 和邮箱 UID 去重、串联回复。
- 先写入 CRM 并保存附件，成功后才标记邮箱邮件已读。
- 新邮件进入受保护询价；已有商机线程的回复直接进入原商机。
- 邮件中心仍由功能开关关闭，不连接真实业务邮箱。

### 验收

- 使用本地假 IMAP 测试重复收取、回复串联、附件和失败重试。
- 同一邮件不会生成两条记录。
- 邮件或商机关联归档后，历史邮件不会被级联删除。
- 未授权人员无法查看邮件正文或下载附件。
- 安全渲染、权限及完整测试通过。

### 确认门

交付后停止，等待：`确认第8步，开始第9步`。

## 第 9 步：CRM 内发送、个人落款和报价包邮件

### 修改范围

- 从询价或商机编写、回复邮件，支持 To、CC、正文和附件。
- 所有邮件通过 `sales@sunkaier.com` 发送。
- 显示名和签名使用实际员工英文姓名、职位和联系方式。
- 无人值守邮件才使用 `SUNKAIER Sales Team`。
- 发送前建立 pending 记录，SMTP 接受后变为 sent，失败保留并可审计重试。
- 正式报价邮件必须绑定已批准的 `QP-Vn`，并冻结收件人、附件和正文。
- 邮件功能开关保持关闭，不连接真实业务邮箱。

### 验收

- 本地假 SMTP 验证发件人、签名、Message-ID、回复头和附件。
- 技术协助工程师没有发送授权时只能保存草稿。
- 未批准报价包不能被邮件接口发送。
- 失败重试不会创建重复的正式报价版本。
- 中英文页面、角色矩阵和完整测试通过。

### 确认门

交付后停止，等待：`确认第9步，开始第10步`。

## 第 10 步：本地端到端验收与发布候选

### 修改范围

- 使用测试数据走完：线索 -> 询价 -> 分配 -> 商机 -> 工程协作 ->
  模板方案 -> 技术审批 -> 报价 V1/V2 -> 邮件发送/回复 -> 接受版本。
- 检查权限、并发、CSRF、上传限制、HTML 安全、审计和错误恢复。
- 验证数据库和附件备份，进行一次隔离恢复演练。
- 更新新加坡专用运维清单，但不执行远程部署。
- 形成可复现的发布包、Git commit、版本标签候选和 SHA-256。

### 验收

- 全角色端到端测试、完整自动化测试和浏览器验收通过。
- 发布包只来自确认后的 Git commit，不包含 `.env`、临时文件或本地附件。
- 回退步骤和数据兼容说明清楚。
- 给用户展示发布内容、风险和启用后的变化。

### 确认门

交付后停止。只有用户明确回复
`确认部署第10步的发布候选到新加坡，邮件保持关闭`
才进入第 11 步。

## 第 11 步：新加坡暗部署，邮件保持关闭

### 修改范围

- 实时确认 `crm.sunkaier.com` 指向的新加坡公网 IP。
- 核对 SSH 用户、应用路径、systemd、PostgreSQL、附件和备份路径。
- 完整备份新加坡数据库、附件、环境和当前代码版本。
- 上传并校验发布包 SHA-256，使用版本目录和原子切换部署。
- 执行迁移，但保持邮件接收和发送功能开关关闭。
- 验证现有登录、客户、询价、商机、附件和审批不回归。
- 不登录、不修改、不重启南京 CRM。

### 验收

- 服务正常，健康检查和 HTTPS 登录正常。
- 数据量和关键记录部署前后对账一致。
- 附件可预览/下载，目录属主和权限正确。
- 新模块可在管理员控制下测试，但不会访问真实邮箱。
- 备份目录、当前版本和一键代码回退点明确。

### 确认门

交付后停止。只有用户明确回复
`确认第11步，允许配置测试邮箱但不启用正式邮箱`
才进入第 12 步。

## 第 12 步：邮箱测试、正式启用和观察

### 修改范围

- 用户自行输入 NetEase 客户端授权密码；执行者不得查看或回显。
- 先使用非客户测试地址完成一收一发和回复串联。
- 验证正文、附件、个人签名、权限、线程、报价版本和审计。
- 用户再次明确确认后才启用正式 IMAP 轮询和 CRM 发信。
- 启用后观察服务、队列、失败重试、磁盘、备份和异常日志。

### 验收

- 测试邮件成功收发并正确归档到测试询价/商机。
- NetEase 授权密码不出现在日志、数据库、页面或 Git 中。
- 每日数据库和附件备份运行，且异机备份和恢复抽查有效。
- 邮件 worker 故障不会停止 CRM Web 服务。
- 提供最终版本、备份、回退点和运行状态报告。

### 最终确认门

只有用户明确回复 `确认启用正式邮件收发`，才打开正式邮件功能开关。

## 5. 每一步的固定交付报告模板

```text
步骤：第 N 步
状态：已完成 / 未完成 / 被阻塞
修改文件：
数据库迁移：
权限变化：
针对性测试：
完整测试：
浏览器验收：
Git commit：
生产影响：无 / 有（详细说明）
回退方法：
残余风险：
下一步：等待用户确认
```

## 6. 当前停点

- 第 1 步“固化本地基线与双语基础”已经在本地完成。
- 第 2 步“所有新商机统一经过询价”已经形成 Git 检查点 `23c0aeb`。
- 第 3 步“商机工程协作角色”已经形成 Git 检查点 `44f415c`。
- 第 4 步“产品技术协议模板库的数据与权限基础”已经在本地完成：
  - 建立模板、模板修订、受控变量、修订变量快照、标准条款和审计事件的数据模型；
  - 模板可以按产品族、型号、应用和语言分类，修订使用 `TPL-R1`、`TPL-R2` 编号；
  - 模板和条款执行草稿、待审核、已发布、已停用状态流转，已提交或已发布内容不可覆盖；
  - 管理员维护安全变量目录和账号权限，但不能发布技术内容；
  - 技术经理负责新建、修订、提交、发布和停用模板及标准条款；
  - 报价工程师只能读取已发布且仍启用的当前版本，销售人员及其他未授权角色直接访问返回拒绝；
  - 修订保存变量定义快照，后续调整全局变量目录不会改变已发布模板历史；
  - 变量键、类型和数据源使用白名单，拒绝脚本、宏及模板执行标记。
- 第 4 步 Git 检查点：`6baac1a`。
- 第 5 步“模板编辑器与快速生成技术方案”已经在本地完成：
  - 新增中英文结构化模板编辑器，支持章节排序、说明文字、参数表、设备表、材料表、仪控表、电气表、公用工程表和供货/接口矩阵；
  - 条件章节只允许使用受控变量和安全运算符，不执行脚本、宏或任意模板表达式；
  - 主任工程师可以从当前启用且已发布的模板生成 `TS-D1` 等独立项目草稿；
  - 每份项目草稿冻结模板修订、双语章节、变量定义和值、选中条款、渲染内容和来源元数据；
  - 自动预填 CRM 已知的客户、联系人、商机、产品、需求、交货地点及负责人信息，工程参数保留人工复核；
  - 缺失必填值、数据类型、允许值和数值范围均有校验，存在问题时不能标记为可提交；
  - 主任工程师可维护全部项目内容并分配章节，协助工程师只能编辑被分配章节和对应变量，销售只能查看；
  - 标准章节内容及条款选择的变化均写入不可追加修改的贡献事件。
- 数据库迁移：`029_product_technical_templates.sql`、`030_opportunity_technical_drafts.sql`。
- 完整测试 `518/518` 通过。
- 浏览器已验证中英文结构化模板、草稿生成入口、主任工程师全量编辑、协助工程师章节权限、销售只读和零控制台错误。
- 本次没有部署、没有连接真实邮箱、没有修改新加坡数据库，也没有影响南京 CRM。
- 本机 Docker Desktop 未运行且没有本地 `psql`，因此迁移 `030` 尚未在真实 PostgreSQL 实例执行；首次进入测试数据库前必须先备份并执行迁移验收。
- 第 6 步“技术方案版本、审批和文档输出”已经在本地完成：
  - 工作草稿使用 `TS-D1`、`TS-D2`，主任工程师可提交、撤回，技术经理可批准或退回；
  - 批准时在商机内串行分配 `TS-V1`、`TS-V2`，退回时保留原快照并复制下一份可编辑 `TS-D`；
  - 提交后的草稿、分工和结构化内容被冻结，已批准版本及其文件不可修改或删除；
  - 批准事务同时生成带 SUNKAIER 品牌、文档编号、版本、页码和审批记录的中英双语 DOCX/PDF；
  - CRM 保存文件二进制、大小和 SHA-256 校验和，并按商机查看权限提供下载；
  - 商机旧版自由文本技术提交入口在版本化仓储启用时关闭，统一进入项目技术草稿流程；
  - 技术经理具有提交审核权限，销售人员只能查看已获授权商机的版本和批准文件，不能编辑或审批。
- 数据库迁移：`031_technical_solution_versions_and_documents.sql`。
- 完整测试 `534/534` 通过；生产依赖审计没有高危或严重漏洞，5 个中危项来自既有 Express/qs 和腾讯云 SDK/uuid 依赖链，不由本步骤新增的 DOCX/PDF 组件引入。
- DOCX 三页和 PDF 两页已经逐页检查，双语文字、表格、审批附录、页码和分页无缺字、乱码、重叠或越界。
- 浏览器已验证中文和英文页面、主任工程师提交/撤回、技术经理审批/退回、批准文件校验和，以及销售只读权限；控制台没有警告或错误。
- 本次没有部署、没有修改新加坡或南京 CRM。
- 本机没有可用的真实 PostgreSQL 测试实例，因此迁移 `031` 尚未执行数据库级验收；新加坡部署前还需安装 Noto CJK 字体或设置 `TECHNICAL_DOCUMENT_FONT_PATH`。
- 第 7 步“完整报价包 V1、V2 版本管理”已经在本地完成：
  - 销售负责人从准确的已批准 `TS-Vn` 和商务报价版本创建 `QP-Dn`，系统同时冻结币种、总价、交期、付款、有效期、报价明细、包含/不含项、技术假设和附件；
  - 技术方案生成文件与商务报价附件均保存来源关联、大小和 SHA-256 校验和，提交后不可替换；
  - 商务经理审批时按商机加事务锁并分配 `QP-V1`、`QP-V2`，避免并发重复编号；
  - 正式修订必须从当前已发版本创建，并同时填写内部修订原因和客户可读变更摘要；页面对比价格、交期、付款、有效期、条款、技术/商务版本及附件校验和；
  - 发出新版本会把上一已发版本标记为“已替代”，已发出、已替代和已接受版本不可修改或删除；
  - 客户接受动作把准确 `QP-Vn` 绑定到商机，报赢和合同审批均要求已接受报价包，合同审批记录保存该版本外键；
  - 销售负责人负责创建、编辑、提交、记录发出和客户接受；只有指派的商务经理可以审批；其他商机成员只读，未获商机权限者直接访问返回拒绝；
  - 目前“记录为已发送”只用于人工发送凭证；第 9 步接入 CRM 邮件后，将由真实发送事务自动记录并关联邮件正文、收件人和附件。
- 数据库迁移：`032_quotation_package_versions.sql`。
- 完整测试 `551/551` 通过。
- 浏览器已验证中文和英文报价包列表/详情、`QP-D2` 提交、商务经理审批为 `QP-V2`、V1/V2 差异、冻结明细和附件校验和；控制台 0 错误、0 警告。
- 本次没有部署、没有连接真实邮箱、没有修改新加坡或南京 CRM。
- 本机没有可用的真实 PostgreSQL 测试实例，因此迁移 `032` 尚未执行数据库级验收；部署测试环境前必须先备份并运行迁移与回退演练。
- 第 8 步“邮件接收、会话和永久归档基础”已经在本地完成：
  - 新增邮件会话、邮件、附件和投递尝试数据结构，保存 RFC `Message-ID`、`In-Reply-To`、`References`、邮箱 UIDVALIDITY/UID、正文、地址、附件路径、大小和 SHA-256；
  - IMAP 收件先完成 CRM 消息及附件归档，全部成功后才标记邮箱邮件已读；归档失败保持未读，后续轮询可对同一消息补齐重试；
  - 以 `Message-ID` 为主、邮箱 UID 身份为辅去重；回复头明确命中历史消息时进入原会话，已转换询价的后续回复自动沿用原商机关联；
  - 无回复关联的新邮件只创建受保护询价，不会根据发件地址直接附着旧商机，也不会自动创建客户或商机；
  - 数据库禁止删除邮件会话、消息、附件和投递尝试，禁止修改入站邮件及归档附件；询价、商机、客户或联系人归档/删除不会级联删除邮件历史；
  - 未关联和询价关联邮件仅管理员、销售经理可见；商机关联邮件沿用商机成员权限；列表、详情、直接 URL 和附件下载统一执行服务层授权；
  - 邮件正文默认以转义后的纯文本显示，原始 HTML 只归档不渲染，远程内容和不安全标记保持阻止；
  - `CRM_EMAIL_CENTER_ENABLED=false` 与 `EMAIL_INTAKE_ENABLED=false` 仍为默认值，未连接真实 NetEase 邮箱。
- 数据库迁移：`033_email_center_archive.sql`。
- 完整测试 `572/572` 通过；本地假 IMAP 已覆盖重复收取、回复串联、附件归档失败和补偿重试。
- 浏览器已验证中文和英文邮件列表/详情、受保护关联、纯文本安全渲染及附件校验和；控制台 0 错误、0 警告。
- 本次没有部署、没有读取或修改邮箱密码、没有连接真实业务邮箱，也没有修改新加坡或南京 CRM。
- 本机没有可用的真实 PostgreSQL 测试实例，因此迁移 `033` 尚未执行数据库级验收；测试环境部署前必须备份并完成迁移与回退演练。
- 第 9 步“CRM 内发送、个人落款和报价包邮件”已经在本地完成：
  - 授权人员可从询价、商机或现有邮件会话编写和回复邮件，支持 To、CC、正文、普通附件及准确的已批准 `QP-Vn`；
  - 所有客户邮件固定通过 `sales@sunkaier.com` 发出，显示名、落款、职位和联系方式来自实际员工资料；缺少英文姓名或职位时禁止发送，不以通用销售团队掩盖实际发件人；
  - 支持工程师默认只能保存草稿，只有获得该商机外发邮件授权后才能发送；销售、商务和其他角色仍受询价及商机可见性约束；
  - 发送状态执行 `draft -> pending -> sent/failed`，每次尝试均保留审计记录；失败可在原邮件上重试并保持同一 Message-ID，不会重复创建报价版本；
  - 正式报价邮件只接受同一商机中已批准的报价包，发送前冻结收件人、正文和附件副本并校验 SHA-256；SMTP 成功后在同一事务中将邮件和准确的报价包版本关联为已发送；
  - 原有人工“记录为已发送”入口已关闭，避免没有邮件证据的报价状态；
  - `CRM_EMAIL_CENTER_ENABLED=false`、`CRM_EMAIL_SENDING_ENABLED=false` 仍为默认值，未连接真实 SMTP 或 NetEase 企业邮箱。
- 数据库迁移：`034_customer_email_sending.sql`。
- 完整测试 `581/581` 通过；本地假 SMTP 已覆盖固定发件箱、员工签名、回复头、报价附件、失败重试和支持工程师草稿限制。
- 浏览器已验证中英文写信与邮件详情、准确 `QP-V2`、个人英文落款、稳定 Message-ID、回复头、附件校验和，以及协助工程师只有“保存草稿”；控制台 0 错误、0 警告。
- 本次没有部署、没有连接真实邮箱、没有读取或修改邮箱密码，也没有修改新加坡或南京 CRM。
- 本机没有可用的真实 PostgreSQL 测试实例，因此迁移 `034` 尚未执行数据库级验收；第 10 步发布候选前必须先在隔离测试数据库备份、迁移并完成回退演练。
- 下一项只能是“第 10 步：本地端到端验收与发布候选”。
- 等待用户明确回复 `确认第9步，开始第10步`。
