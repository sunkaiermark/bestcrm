<%- include('../partials/app-start', { title: `${t('analytics')} - BESTCRM`, activeNav: 'analytics' }) %>

<header class="page-header">
  <div>
    <h1><%= t('analytics') %></h1>
  </div>
</header>

<section class="form-panel">
  <p class="muted"><%= t('noAnalyticsReports') %></p>
</section>

<%- include('../partials/app-end') %>
