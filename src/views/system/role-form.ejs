<%- include('../partials/app-start', { title: `${mode === 'new' ? 'New Role' : 'Edit Role'} - BESTCRM`, activeNav: 'system-roles' }) %>
        <p class="breadcrumb"><a href="/system/roles">System Roles</a> / <%= mode === 'new' ? 'New Role' : 'Edit Role' %></p>
        <header class="page-header">
          <h1><%= mode === 'new' ? 'New Role' : 'Edit Role' %></h1>
        </header>

        <form class="form-panel" method="post" action="<%= action %>">
          <% if (mode === 'new') { %>
            <label>
              Role code
              <input name="code" value="<%= role.code || '' %>" required pattern="[a-z][a-z0-9_]*">
            </label>
          <% } else { %>
            <label>
              Role code
              <input value="<%= role.code %>" disabled>
            </label>
          <% } %>

          <label>
            Role name
            <input name="name" value="<%= role.name || '' %>" required>
          </label>
          <label>
            Description
            <textarea name="description"><%= role.description || '' %></textarea>
          </label>

          <label class="checkbox-row">
            <input name="isActive" type="checkbox" <%= role.isActive ? 'checked' : '' %>>
            <span>Active role</span>
          </label>

          <div class="inline-actions">
            <button type="submit"><%= mode === 'new' ? 'Create role' : 'Save role' %></button>
            <a class="secondary-action" href="/system/roles">Cancel</a>
          </div>
        </form>
<%- include('../partials/app-end') %>
